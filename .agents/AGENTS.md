# Project Rules

- **Git Commands Restriction**: Do NOT automatically execute `git push` command-line runs. Only propose/run local commits or local builds, and leave the `git push` execution as an explicit action for the user, or ask for confirmation first.
